---
  - name: Init
    hosts:
      - web_servers
      - db_servers
    become: yes
    roles:
      - init

  - name: Database server
    roles:
      - mysql
    become: yes
    hosts:
      - db_servers

  - name: Web server
    hosts: web_servers
    become: yes
    roles:
      - agama
      - uwsgi
      - nginx

  - name: Collect info about all VMs
    hosts: all
    tasks:
      - setup:

  - name: DNS servers
    hosts: dns_server
    become: yes
    roles:
      - bind9

  - name: Resolv.conf to both machines
    hosts:
      - web_servers
      - dns_server
    become: yes
    roles:
      - resolv
