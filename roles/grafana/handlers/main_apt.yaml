---
  - name: restart grafana
    service:
      name: grafana-server
      state: restarted

  - name: Reset Grafana Password
    shell: |
      sleep 60
      grafana-cli admin reset-admin-password {{ grafana_pass }}
    # no_log: false
