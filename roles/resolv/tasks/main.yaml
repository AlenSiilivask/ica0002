---
  - name: stop systemd-resolved
    service:
      name: systemd-resolved
      state: stopped
      enabled: no

  - name: move resolf.conf
    template:
      src: resolv.conf
      dest: /etc/resolv.conf
